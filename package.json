{
  "name": "e-citoyen-td",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd .. && mkdir -p build && cp -r frontend/dist/. build/"
  }
}
